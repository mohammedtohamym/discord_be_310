# Copy this file to .env and customize the values for your environment
API_PORT=5002

# For local development you can use a local MongoDB instance or MongoDB Atlas connection string
# Example local connection string (requires MongoDB running locally or via Docker)
MONGO_URI=mongodb://127.0.0.1:27017/discord

# Replace with a strong, random string used to sign JWT tokens
TOKEN_KEY=replace_with_strong_token_key
